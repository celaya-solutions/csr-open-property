[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm typecheck && pnpm build"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
